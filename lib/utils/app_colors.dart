import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF0066F9);
  static const Color lightBlue = Color(0xFFCAD8FF);
  static const Color white = Color(0xFFFFFFFF);

  // Additional derived colors for better UX
  static const Color primaryLight = Color(0xFF3385FA);
  static const Color primaryDark = Color(0xFF0052CC);
  static const Color lightBlueLight = Color(0xFFE1E9FF);
  static const Color lightBlueDark = Color(0xFFB8C7FF);

  // Semantic colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Text colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textLight = Color(0xFFBDBDBD);

  // Background colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color backgroundLight = Color(0xFFF5F7FA);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color divider = Color(0xFFE0E0E0);

  // Shadow colors
  static const Color shadow = Color(0x1A000000);
  static const Color shadowLight = Color(0x0A000000);
}
